import { StyleSheet } from 'react-native';
import * as constants from '../../../../styles/constants.js';

export default generalStyles = StyleSheet.create({
  drawerItemIcon: {
    height: 20,
    width: 20,
    marginLeft: constants.grid.xs,
  }
});
